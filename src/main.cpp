#include "editor.h"

/**
 * @file main.cpp
 * @brief Entry point for the text editor application.
 * 
 * Creates an Editor instance and starts the main program loop.
 */

int main() {
    Editor editor;
    editor.run();
    return 0;
}
